package gui.panes;

import managers.GUIManager;

public class MenuPane extends CustomLayerPane {

    public MenuPane(){

        getLayeredPane().setBounds(0, 0,GUIManager.getWidth(), GUIManager.getHeight());


    }
}
